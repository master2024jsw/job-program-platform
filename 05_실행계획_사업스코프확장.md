# 05. 실행계획 — 사업 스코프 확장 (심사① 대응) v2

> [04_현황_갭분석.md](04_현황_갭분석.md) 5절 "11/14까지 권장 작업 순서" 1번 작업의 구현 계획.
> 대응 심사 지적: ① 로그인 시점부터 사업 구분, 사업별 증빙서류 양식 식별
> 관련 요구사항: REQ3(지침 개정을 코드 수정 없이 반영), REQ4(실무자 최종 검토)

## v1 → v2 변경 요약

| # | 변경 | 이유 |
|---|---|---|
| 1 | 필수서류 목록: shared 상수 → **서버 JSON 데이터 파일 + API** | REQ3. 지침 개정 시 코드 수정 없이 반영 |
| 2 | 필수서류를 **단계(기업적격/참여자적격/지원금검증) × 대상(기업/근로자)**으로 구분, 사업계획서 제외 | 사업 단위 체크로는 기업·근로자별 제출 여부를 구분할 수 없음. 사업계획서는 세대통합형 전용 |
| 3 | **서류유형 코드값** 도입, `Document.documentType`을 코드로 관리 | 현재 AI가 자유 텍스트로 기록해 체크리스트와 연결 불가 |
| 4 | 체크리스트를 **선택한 기업/근로자 기준**으로 표시 | 2와 동일 |
| 5 | businessId 요청값을 서버에서 **UserBusiness로 권한 검증** | 미배정 사업 데이터 조회 차단 |
| 6 | 메일 자동수집 사업 지정: 수집 시점 선택 사업 → **발신자→기업→CompanyBusiness 매칭** | 선택 사업으로 일괄 지정 시 오분류 |
| 7 | 기존 데이터 UPDATE 전 **실제 businesses.id 조회** 절차 추가 | `BIZ-2026-SENIOR`는 추정값 |
| 8 | 사업 선택: 별도 페이지 → **로그인 화면 드롭다운** | 심사① "로그인 시점부터" 문구에 직접 대응 |
| 9 | SubsidySetting: 사업별 행이 없으면 **기본값 자동 생성** | 신규 사업 등록 시 오류 방지 |
| 10 | 사업 유형 3종으로 정리, SENIOR 표시명 변경 | 사용자 확정 |

---

## 1. 확정 사항

### 1.1 사업 유형 코드·표시명

| 순서 | typeCode | 표시명 | 상태 |
|---|---|---|---|
| 1 | `SENIOR` | 현장실습훈련(시니어인턴십) | 활성 |
| 2 | `YOUTH` | 청년일자리도약장려금 | 준비중 |
| 3 | `WORK_EXP` | 청년미래내일일경험 | 준비중 (신규 코드) |

- `packages/shared`의 `BUSINESS_TYPE_CODES` / `BUSINESS_TYPE_LABELS`를 위 순서·명칭으로 정리
- 기존 데이터의 `typeCode` 값은 변경하지 않음 (라벨만 변경)
- 표시명은 로그인 드롭다운, 상단바 사업 전환, SetupPage 등 **라벨이 나오는 모든 곳에 동일 적용**
- "준비중" 판단 기준: 해당 typeCode의 필수서류 데이터 파일이 없거나, 기관에 해당 사업이 등록되지 않은 경우

### 1.2 로그인 화면

```
┌──────────────────────────────────┐
│             잡도리 AI             │
│  로그인ID  [__________________]  │
│  비밀번호  [__________________]  │
│  사업 선택 [▼ 현장실습훈련(시니어인턴십)]│
│                      [ 로그인 ]  │
└──────────────────────────────────┘
```

- 로그인ID, 비밀번호 아래에 "사업 선택" 드롭다운을 두고 **한 번에 로그인**
- 드롭다운은 1.1 표의 3개를 순서대로 고정 표시, 기본값은 현장실습훈련(시니어인턴십)
- 준비중 항목은 `(준비중)`을 붙여 표시하고 선택 불가
- 별도 `BusinessSelectPage`는 만들지 않음
- 선택값은 `sessionStorage`에 유지, 새로고침 시 다시 묻지 않음
- 로그인 후 상단바 사업 전환 드롭다운은 그대로 유지

### 1.3 로그인 서버 처리

- 로그인 API가 `businessTypeCode`를 함께 받음
- 처리 순서
  1. ID·PW 인증
  2. 기관 내 해당 typeCode 사업 조회 (없으면 "등록되지 않은 사업입니다")
  3. `UserBusiness`로 배정 여부 확인 (관리자 예외). 미배정이면 "선택한 사업에 배정되지 않은 계정입니다"
  4. 성공 시 해당 사업을 `currentBusinessId`로 응답 → 바로 그 사업 화면으로 진입
- 한 기관에 같은 typeCode 사업이 연도별로 여러 개면 **가장 최근 baseYear**를 선택

### 1.4 서류유형 코드

| 코드 | 서류명 | 비고 |
|---|---|---|
| `COMPANY_APPLICATION` | 참여기업 신청서 | |
| `BUSINESS_REGISTRATION` | 사업자등록증 | 필수서류 목록 외, 분석용 |
| `PARTICIPANT_APPLICATION` | 참여신청서 | |
| `PRIVACY_CONSENT` | 개인정보 수집·이용 동의서 | |
| `HEALTH_INSURANCE_HISTORY` | 건강보험자격득실확인서 | |
| `PAYSLIP` | 임금명세서 | |
| `EMPLOYMENT_CONTRACT` | 근로계약서 | |
| `ATTENDANCE_RECORD` | 출근부 | |
| `OTHER` | 기타 | 분류 불가 시 |

- 코드 목록도 데이터 파일로 관리 (`apps/server/resources/document-types.json`)
- `Document.documentType`에는 코드값 저장, 화면에는 서류명 표시
- 지정 방법
  - 업로드 시: 드롭다운으로 선택 (선택 안 하면 AI 분류)
  - AI 분석 시: Gemini 프롬프트에 코드 목록을 제시하고 **목록 안에서만** 고르게 제한, 해당 없으면 `OTHER`
  - 실무자가 문서함에서 언제든 수정 가능 (REQ4)
- 기존 자유 텍스트 값은 그대로 두고, 코드 목록에 없는 값은 화면에서 "미분류"로 표시

### 1.5 필수서류 데이터 (SENIOR 2026)

파일: `apps/server/resources/required-documents/SENIOR-2026.json`

```json
{
  "typeCode": "SENIOR",
  "guidelineYear": 2026,
  "source": "2026년도 현장실습 훈련 지원사업 운영안내(한국노인인력개발원)",
  "stages": [
    {
      "stage": "COMPANY_ELIGIBILITY",
      "label": "기업 적격 확인",
      "target": "COMPANY",
      "documents": ["COMPANY_APPLICATION"]
    },
    {
      "stage": "PARTICIPANT_ELIGIBILITY",
      "label": "참여자 적격 확인",
      "target": "WORKER",
      "documents": ["PARTICIPANT_APPLICATION", "PRIVACY_CONSENT", "HEALTH_INSURANCE_HISTORY"]
    },
    {
      "stage": "SUBSIDY_VERIFICATION",
      "label": "지원금 신청 서류검증(일반형)",
      "target": "WORKER",
      "documents": ["PAYSLIP", "EMPLOYMENT_CONTRACT", "ATTENDANCE_RECORD"]
    }
  ]
}
```

- 사업계획서(세대통합형), 청년멘토 활동보고서, 예외서류는 **이번 범위에서 제외** — 참여유형별 분기는 검증엔진 작업(04문서 순서2)에서 처리
- 서버는 기동 시 파일을 읽어 캐시하고 API로 제공: `GET /required-documents?typeCode=SENIOR`
- YOUTH, WORK_EXP는 파일이 없으므로 준비중으로 판단

### 1.6 businessId 전달·권한

- 프론트가 매 요청마다 쿼리/바디로 `businessId` 전달 (`CompaniesController` 기존 패턴과 통일, 다중 탭 대응)
- 서버에 **공통 헬퍼 또는 가드**를 두어 요청 사용자가 해당 사업에 배정되어 있는지 `UserBusiness`로 확인
  - 관리자(admin 역할)는 기관 내 모든 사업 허용
  - 미배정이면 403
- 신규 생성 DTO에서 `businessId` 필수

### 1.7 메일 자동수집 사업 매칭

1. 발신자 이메일 → 기업(또는 근로자) 매칭 (기존 로직)
2. 매칭된 기업의 `CompanyBusiness` 조회
3. 사업이 **1개** → 해당 businessId 자동 지정
4. **0개 또는 2개 이상** → `businessId = NULL`, 문서함 "미분류" 탭에 표시
5. 실무자가 미분류 문서에서 사업을 지정하면 해당 사업으로 이동

### 1.8 SubsidySetting

- 싱글턴(`id='default'`) 폐기 → `businessId` unique 컬럼, 사업별 1행
- 조회 시 해당 사업 행이 없으면 **기본값(eligibilityMonths = 3)으로 자동 생성** 후 반환
- 기존 `default` 행의 값은 SENIOR 사업 행 생성 시 초기값으로 사용

---

## 2. 엔티티 변경안

공통 패턴 (`Document` / `Worker` / `MailLog` / `SubsidyCalculation`):

```ts
@Column({ name: 'business_id', type: 'varchar', nullable: true })
@Index()
businessId?: string | null;
```

- 관계(ManyToOne) 없이 컬럼만 추가 (기존 `companyId`/`workerId` 관례와 동일)
- DB 컬럼은 nullable(과거 데이터 보존), 신규 생성 DTO에서는 필수

`SubsidySetting`:

```ts
@PrimaryGeneratedColumn('uuid')
id!: string;

@Column({ name: 'business_id', unique: true })
businessId!: string;
```

---

## 3. 기존 데이터 처리

`synchronize: true`(dev)이므로 서버 재기동 시 컬럼이 추가되고 기존 행은 `business_id = NULL`.
마이그레이션 프레임워크는 이번에 도입하지 않음 (04문서 순서5 범위).

C1 완료 후 1회성으로 sqlite3에서 실행 (코드에 포함하지 않음):

```sql
-- 1) 실제 사업 ID 먼저 확인
SELECT id, name, type_code, base_year FROM businesses;

-- 2) 확인한 SENIOR 사업 ID로 치환해서 실행
UPDATE documents            SET business_id = '<확인한 ID>' WHERE business_id IS NULL;
UPDATE workers              SET business_id = '<확인한 ID>' WHERE business_id IS NULL;
UPDATE mail_logs            SET business_id = '<확인한 ID>' WHERE business_id IS NULL;
UPDATE subsidy_calculations SET business_id = '<확인한 ID>' WHERE business_id IS NULL;
```

NULL로 남아도 앱은 동작함 (해당 사업 스코프에서 보이지 않을 뿐, 문서는 미분류 탭에 표시).

---

## 4. 변경 파일 목록

**신규**
- `apps/server/resources/document-types.json`
- `apps/server/resources/required-documents/SENIOR-2026.json`
- `apps/server/src/modules/required-documents/` (모듈·서비스·컨트롤러)
- `apps/server/src/common/business-access.guard.ts` (또는 헬퍼)
- `apps/renderer/src/api/required-documents.ts`
- `apps/renderer/src/components/RequiredDocumentsChecklist.tsx`

**수정 — shared**
- `packages/shared/src/types/business.ts` (유형 코드·표시명)
- `packages/shared/src/types/document.ts` (서류유형 코드 타입)

**수정 — 백엔드**
- `auth/auth.controller.ts`, `auth.service.ts`, `dto/login.dto.ts`
- `documents/`: entity, create/update DTO, service, controller, `gemini.service.ts`(프롬프트)
- `workers/`: entity, create DTO, service, controller
- `mail/`: `mail-log.entity.ts`, `send-mail.dto.ts`, service, controller
- `subsidy/`: calculation·setting entity, DTO 2종, service, controller
- `mail-collector/mail-collector.service.ts`

**수정 — 프론트엔드**
- `pages/LoginPage.tsx`, `auth/AuthContext.tsx`, `App.tsx`
- `api/documents.ts`, `api/workers.ts`, `api/mail.ts`, `api/subsidy.ts`
- `pages/DocumentsPage.tsx`, `WorkersPage.tsx`, `MailPage.tsx`, `SubsidyPage.tsx`, `SetupPage.tsx`
- `components/AppShell.tsx` (상단바 라벨)

---

## 5. 작업 단계 (커밋 단위)

| 단계 | 내용 | 커밋 메시지 | 동작 확인 |
|---|---|---|---|
| A | 사업 유형 3종·표시명 정리, 서류유형 코드 JSON, 필수서류 JSON, 조회 API | `사업유형 3종 정리 및 서류유형·필수서류 데이터 파일 추가` | `GET /required-documents?typeCode=SENIOR` 응답 확인 |
| B | 로그인 화면 사업 드롭다운 + 서버 배정 검증 | `로그인 화면에서 사업 선택 후 입장하도록 변경` | 드롭다운 3개 표시, 준비중 선택 불가, 미배정 사업 로그인 시 오류 |
| C1 | 5개 엔티티 businessId, 권한 가드, SubsidySetting 자동 생성, 메일수집 사업 매칭 | `문서·근로자·메일·지원금 데이터에 사업 스코프 적용 (백엔드)` | API에 다른 사업 ID로 요청 시 403, 신규 사업 지원금 설정 자동 생성 |
| C2 | 프론트 화면 연동, 문서함 미분류 탭 | `문서·근로자·메일·지원금 화면을 현재 사업 기준으로 연동` | 상단바에서 사업 전환 시 목록이 바뀜 |
| D | 서류유형 코드 적용 (업로드 드롭다운, AI 분류 제한, 실무자 수정) | `서류유형을 코드값으로 관리하고 업로드·AI분석에 적용` | 업로드 시 선택, AI 분석 결과가 코드 목록 안에서만 나옴 |
| E | 필수서류 체크리스트 (기업/근로자 선택 → 단계별 제출·미제출) | `문서함에 기업·근로자별 필수서류 체크리스트 표시` | 기업 선택 시 기업적격 서류, 근로자 선택 시 참여자·지원금 서류 제출 여부 표시 |

- A·B는 서로 독립, 먼저 진행
- C1·C2는 같은 세션에서 연속 커밋 (중간에 앱이 깨진 상태로 두지 않기)
- D는 E의 전제 조건
- 각 단계 완료 후 변경 내용을 보여주고 승인받은 뒤 커밋

## 6. 이번 범위에서 하지 않는 것

- 참여유형(일반형/세대통합형/장기취업유지형)별 필수서류 분기 → 검증엔진 작업
- YOUTH, WORK_EXP 필수서류 데이터 → 지침 확보 후 JSON 추가만으로 활성화
- TypeORM 마이그레이션 도입 → 04문서 순서5
- ESLint 설정

## 7. 진행 상태

- [ ] A. 사업유형·서류유형·필수서류 데이터 + API
- [ ] B. 로그인 화면 사업 드롭다운
- [ ] C1. businessId 스코프 — 백엔드
- [ ] C2. businessId 스코프 — 프론트엔드
- [ ] D. 서류유형 코드 적용
- [ ] E. 필수서류 체크리스트 UI
